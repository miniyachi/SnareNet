# @package _global_
# SnareNet for Convex QCQP dataset
# Usage: python3 run_experiment.py +experiment=lambd dataset=cvx_qcqp model=snarenet

epochs: 2000
batch_size: 200
learning_rate: 0.0001

model:
  trust_region: false
  adaptive_relaxation: true
  decay_epochs: 500
  rtol: 1.0e-8
